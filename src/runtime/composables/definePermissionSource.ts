import type { PermissionSource } from '../types'

/** Define the data source (default export of `<srcDir>/permission.ts`). Type helper only; auto-imported. */
export function definePermissionSource(source: PermissionSource): PermissionSource {
    return source
}
